export type IUserProps = {
  id: string,
  name: string
  token: string
}

export type IBookProps = {
  title: any,
  author: string,
  coverImageUrl: string,
  id: string,
  pageCount: number,
  publisher: string,
  synopsis: string
}
